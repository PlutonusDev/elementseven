DATABASE_URL="postgresql://element7:element7@localhost:5432/element7"

AUTH_SECRET="replace-with-openssl-rand-base64-32"
# Production: https://elementseven.net — use http://localhost:3000 for local dev
NEXTAUTH_URL="https://elementseven.net"
APP_URL="https://elementseven.net"

STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# SMTP transactional email. Leave blank to log emails to the console instead.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
# "true" for implicit TLS (port 465), "false" for STARTTLS (587). Omit to infer from port.
SMTP_SECURE=""
EMAIL_FROM="Element Seven <orders@elementseven.net>"

# One-time secret to promote the logged-in account to ADMIN via /api/admin/claim?token=...
# Generate with: openssl rand -hex 32
ADMIN_CLAIM_SECRET=""
